REACT
before fetching data from API, use custom data to populate
de-structuring instead of props in the function parameter
maps to traverse array

CSS
js logic to add classes
variables instead of direct values
backface-visbility: hidden, property that will hide the values which are rotated 180 degree
transform-style: preserve-3d, should be used with the above property

 